# 闲鱼兼职简历项目 · 制作流程

> 一个自包含（单文件）的个人简历网站模板，带翻页书、像素动画、图片画廊。
> 适用人群：设计师 / 前端 / 产品经理 / 应届生 / 自媒体

---

## 一、技术栈

| 项目 | 内容 |
|---|---|
| 语言 | 纯 HTML + CSS + JavaScript |
| 外部依赖 | Google Fonts (Inter 字体) |
| 部署方式 | Surge.sh（免费二级域名） |
| 文件 | 单文件 `index.html`，所有图片 base64 内嵌 |
| 大小 | 约 1.3MB（含图片数据） |

---

## 二、页面结构

```
index.html
├── 终端顶栏（.topbar）
├── 主布局（.main-wrap）
│   ├── 侧边栏（.sidebar）
│   │   ├── 头像（.avatar）
│   │   ├── 名字 / 标语
│   │   ├── 个人信息（年龄 · MBTI）
│   │   └── 联系方式（邮箱 · GitHub · 微信）
│   │
│   └── 主内容（.content）
│       ├── 自我介绍（introduction）
│       ├── 经验（experience）
│       │   └── 画廊（图片浏览）
│       ├── 项目（projects）
│       │   └── K线图（交易策略展示）
│       ├── 技能（skills）
│       ├── 教育（education）
│       └── 写作（writing）
│           ├── Lil Agents 像素动画
│           └── 翻页书（小说阅读器）
└── 底部标签（终）
```

---

## 三、核心功能模块

### 3.1 图片画廊（Gallery）

**位置：** `index.html` 约 L926-991

**功能：** 点击展开/收起，左右切换图片，缩略图导航，键盘左右键支持。

**关键代码：**

```javascript
const pages = ['03','04','05','07','08','09','10','11']; // 图片文件名编号

function toggleGallery() {
  const wrap = document.getElementById('gallery-wrap');
  wrap.classList.toggle('open');
}

function changeImage(delta) {
  currentIdx = Math.max(0, Math.min(pages.length - 1, currentIdx + delta));
  updateImage();
}
```

**图片数据：** 所有图片在 `IMG_DATA` 对象中 base64 内嵌。

```javascript
var IMG_DATA = {
  'page-03.jpg': 'data:image/jpeg;base64,...',
  'page-04.jpg': 'data:image/jpeg;base64,...',
  // ...
  'girl1-walk.png': 'data:image/png;base64,...',  // 像素精灵图
  'girl2-walk.png': 'data:image/png;base64,...',
  'girl3-walk.png': 'data:image/png;base64,...',
};
```

**接单改内容：**
- 替换 `IMG_DATA` 里对应图片的 base64 数据
- 修改 `pages` 数组里的文件名编号
- 如果图片数量变了，缩略图部分会自动生成

### 3.2 K 线图（交易策略展示）

**位置：** `index.html` 约 L1004-1095

**功能：** 在 canvas 上绘制 K 线图 + 买卖信号标注。纯前端绘制，不需要外部数据源。

**数据结构：**

```javascript
const data = [
  { o: 4750, h: 4850, l: 4700, c: 4820, v: 1.2 }, // 开盘、最高、最低、收盘、成交量
  // ...
];
const signals = { 6: 'buy', 10: 'sell', 15: 'sell', 18: 'buy' }; // 买卖信号
```

**接单改内容：**
- 替换 `data` 数组里的 K 线数据（从交易软件导出）
- 修改 `signals` 里的买卖点坐标
- 调整标题文字和描述

### 3.3 翻页书（小说阅读器）

**位置：** `index.html` 约 L1286-1386

**功能：** CSS 3D 翻页效果，支持上一页/下一页。每页内容为多段落文字，自动渲染。

**关键代码：**

```javascript
const bookData = [
  ["第十五章 蓝天下","段落1...","段落2..."],   // 一页一个数组
  ["段落3...","段落4..."],
  // ...共 12 页
];
```

**翻页动画（CSS 3D）：**
```css
.book-page.flip-out {
  animation: pageFlip 0.65s cubic-bezier(0.645, 0.045, 0.355, 1) forwards;
}
@keyframes pageFlip {
  from { transform: rotateY(0deg); }
  to { transform: rotateY(-178deg); }
}
```

**接单改内容：**
- 替换 `bookData` 里的文本内容
- 每页一个数组，数组里每个字符串是一个段落
- 标题用 `"第十五章 蓝天下"` 或 `"尾声"` 会自动识别为章节标题样式
- `"（全文完）"` 会自动居中加橙色
- 页数自动计算，不需要手动改

### 3.4 像素小人动画（Lil Agents）

**位置：** `index.html` 约 L1151-1284

**功能：** 三个像素小人在 Dock 条上走路，使用 craftpix sprite 贴图。支持精灵图模式和 fallback 绘图模式（防止闪屏）。

**关键配置：**

```javascript
const lilChars = [
  { id: 0, x: 100, speed: 0.6, color: '#7a5a9a' },  // 紫色
  { id: 1, x: 300, speed: 0.4, color: '#4aaf7a' },  // 绿色
  { id: 2, x: 500, speed: 0.5, color: '#e07b3a' },  // 橙色
];
```

**精灵图规格：**
- 精灵图宽高：192×128 像素/帧
- 每张图包含 8 帧（横向排列）
- 完整精灵图尺寸：1536×128
- 显示缩放：0.65

**防闪屏机制：**
```javascript
// 始终先绘制 fallback（彩色圆形人形）
// 精灵图加载完成后无缝切换，尺寸一致，无视觉跳跃
if (img && img.complete && img.naturalWidth > 0) {
  // 精灵图模式：drawImage 裁剪帧
  ctx.drawImage(img, sx, 0, 192, 128, ...);
} else {
  // Fallback 模式：同尺寸绘制
}
// 3 秒超时：精灵图加载失败也不卡死
setTimeout(function() { spritesChecked = true; }, 3000);
```

**接单改内容：**
- 替换 `girl1-walk.png`/`girl2-walk.png`/`girl3-walk.png` 的像素精灵图 base64
- 修改 `lilChars` 数组里的颜色、速度
- 精灵图规格改变时调整 `SPRITE_FW`/`SPRITE_FH`/`SPRITE_SCALE`
- 想加/减小人就增删 `lilChars` 条目和精灵图

### 3.5 侧边栏（个人信息）

**位置：** `index.html` 约 L84-170

**内容：** 头像、姓名、标语、年龄、MBTI、兴趣爱好、联系方式。

**接单改内容：**
- 修改头像（`<div class="avatar">` 里的初始文字）
- 修改姓名、标语、年龄/MBTI
- 修改联系方式（邮箱、GitHub、微信）
- 增删兴趣爱好的 emoji 列表

---

## 四、部署流程

### 4.1 安装 Surge

```bash
npm install -g surge
```

### 4.2 部署

```bash
# 创建部署目录，放入 index.html
mkdir -p /tmp/deploy
cp index.html /tmp/deploy/
cd /tmp/deploy

# 部署（第一次会提示输入邮箱密码注册）
surge --domain xxx-resume.surge.sh .
```

### 4.3 更新

```bash
# 替换 index.html 后重新部署（已登录过就不需要再输入密码）
cd /tmp/deploy
cp /path/to/new/index.html .
surge --domain xxx-resume.surge.sh .
```

### 4.4 自定义域名（可选）

买域名（如 `yangyunhua.com`）后，DNS 配置 CNAME 指向 `xxx-resume.surge.sh`。

---

## 五、图片处理流程

### 5.1 将图片转为 base64

使用在线工具（搜索"图片转 base64"）或命令行：

```bash
# macOS 命令行
base64 -i image.jpg > image.txt
# 然后将输出复制到 IMG_DATA 对象中
```

### 5.2 像素精灵图准备

- 下载 craftpix sprite pack
- 提取每个角色的 `Walk.png`
- 用 Python 脚本切割帧并合成为视频（Mac app 版用，网页版直接用精灵图）
- 精灵图规格：192×128 每帧，8 帧一横行 = 1536×128

---

## 六、接单工作流

```
客户询价 → 确认需求 → 收资料 → 改模板 → 部署 → 发链接

收资料清单：
□ 姓名 / 年龄 / MBTI（可选）
□ 个人介绍文案
□ 头像照片
□ 作品图片（用于画廊）
□ 作品链接 / GitHub
□ 联系方式
□ 想要展示的其他内容（书、博客、项目等）
```

**改模板耗时：**
- 文字替换：5 分钟
- 替换图片：10 分钟
- 部署上线：2 分钟
- 总计：约 20-30 分钟

**定价参考：**
- 基础版（替换文字 + 图片）：¥XXX
- 进阶版（基础 + 翻页书/K线）：¥XXX
- 定制版（全新功能）：¥XXX

---

## 七、常见问题

**Q：客户没有像素精灵图怎么办？**
A：用 fallback 模式的彩色小人就够好看了（彩色圆形人+身体+腿），或者从 freepik 找免费素材。

**Q：客户想改字体/颜色？**
A：改 CSS `:root` 里的变量：
```css
--accent: #e07b3a;     /* 主色 */
--accent2: #d4567a;    /* 辅色 */
--bg: #f8f6f2;         /* 背景 */
```

**Q：页面太大了加载慢？**
A：图片 base64 会导致页面大。如果客户接受分离文件，可以改成外链图片，用 `new Image()` 加载。

**Q：部署链接会失效吗？**
A：Surge 免费版域名永久有效，只要一年内有一次部署就不会回收。建议每半年重新部署一次。

**Q：客户想用自己的域名？**
A：让客户去买域名，DNS 配 CNAME 到 Surge 域名，配置文件找我帮忙配。

---

## 八、文件列表

| 文件 | 用途 |
|---|---|
| `index.html` | 主文件（所有内容自包含） |
| `闲鱼兼职简历项目.md` | 本文档 |

---

> 最后更新：2026-05-15
> 示例网站：https://yyuhua-resume.surge.sh
